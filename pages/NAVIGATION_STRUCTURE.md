# 页面导航结构 - 按四个底部导航栏组织

## 📱 底部导航栏配置

根据 `components/layout/BottomNav.vue` 的配置：

```javascript
const navItems = [
  { label: '社区广场', path: '/', icon: '🗺️' },
  { label: '任务', path: '/tasks', icon: '📋' },
  { label: '活动', path: '/activities-feed', icon: '📅' },
  { label: '我的', path: '/member/1', icon: '👤' }
]
```

---

## 📂 页面文件组织

### 🗺️ 1. 社区广场 (Square)
**导航路径**: `/`  
**主要文件**: `index.vue`

```
pages/
├── index.vue                    # ✅ 社区广场主页 (/)
├── community/                   # ✅ 社区相关
│   ├── [id].vue                #   社区详情 (/community/:id)
│   └── setup.vue               #   社区设置 (/community/setup)
├── dao.vue                      # ✅ DAO管理 (/dao)
└── market.vue                   # ✅ 市场 (/market)
```

**说明**: 
- `index.vue` 是社区广场的主入口，必须保持在根目录以保持路由为 `/`
- `community/` 文件夹包含所有社区相关页面
- `dao.vue` 和 `market.vue` 是社区广场的扩展功能

---

### 📋 2. 任务 (Tasks)
**导航路径**: `/tasks`  
**主要文件**: `tasks/index.vue`

```
pages/
└── tasks/                       # ✅ 任务模块
    ├── index.vue               #   任务列表 (/tasks)
    ├── [id].vue                #   任务详情 (/tasks/:id)
    ├── create.vue              #   创建任务 (/tasks/create)
    └── detail/                 #   任务详情子页面
        ├── submit.vue          #   提交任务 (/tasks/detail/submit)
        └── review.vue          #   审核任务 (/tasks/detail/review)
```

**说明**:
- 所有任务相关页面都组织在 `tasks/` 文件夹中
- 使用嵌套路由 `detail/` 来处理任务详情相关的子页面

---

### 📅 3. 活动 (Activities)
**导航路径**: `/activities-feed`  
**主要文件**: `activities-feed.vue`

```
pages/
├── activities-feed.vue          # ✅ 活动流主页 (/activities-feed)
└── activities/                  # ✅ 活动相关
    ├── index.vue               #   活动列表 (/activities)
    ├── [id].vue                #   活动详情 (/activities/:id)
    └── create.vue              #   创建活动 (/activities/create)
```

**说明**:
- `activities-feed.vue` 是活动流的主入口，保持在根目录以保持路由为 `/activities-feed`
- `activities/` 文件夹包含活动列表、详情和创建页面

---

### 👤 4. 我的 (Member)
**导航路径**: `/member/1`  
**主要文件**: `member/[id].vue`

```
pages/
├── member/                      # ✅ 成员相关
│   └── [id].vue                #   个人主页 (/member/:id)
├── profile/                     # ✅ 个人资料设置
│   └── setup.vue               #   个人资料设置 (/profile/setup)
└── wallet.vue                   # ✅ 钱包 (/wallet)
```

**说明**:
- `member/[id].vue` 是个人主页，使用动态路由
- `profile/` 文件夹包含个人资料相关设置
- `wallet.vue` 钱包页面（重定向到 `/member/:id`）

---

### 🔐 认证相关 (Auth)
**独立页面，不属于底部导航栏**

```
pages/
├── login.vue                    # 登录 (/login)
├── verify.vue                   # 验证 (/verify)
└── verifyphone.vue              # 手机验证 (/verifyphone)
```

---

## 📊 页面组织总结

| 底部导航 | 主入口路径 | 主入口文件 | 相关文件夹 | 状态 |
|---------|-----------|-----------|-----------|------|
| 🗺️ 社区广场 | `/` | `index.vue` | `community/` | ✅ 已组织 |
| 📋 任务 | `/tasks` | `tasks/index.vue` | `tasks/` | ✅ 已组织 |
| 📅 活动 | `/activities-feed` | `activities-feed.vue` | `activities/` | ✅ 已组织 |
| 👤 我的 | `/member/1` | `member/[id].vue` | `member/`, `profile/` | ✅ 已组织 |

---

## ✅ 当前结构评估

**优点**:
1. ✅ 四个底部导航栏对应的页面都已清晰组织
2. ✅ 主要入口页面保持在正确位置，路由路径正确
3. ✅ 相关功能的子页面都组织在对应的文件夹中
4. ✅ 使用动态路由 `[id].vue` 和嵌套路由 `detail/` 结构清晰

**建议**:
- 当前结构已经很好地按照四个底部导航栏组织
- 不需要移动主要入口文件（会破坏路由）
- 可以保持现有结构，或根据需要创建逻辑分组文件夹（但不移动文件）

---

## 🔗 路由引用检查

所有页面中的路由引用应该指向：
- 社区广场: `/` 或 `navigateTo('/')`
- 任务: `/tasks` 或 `navigateTo('/tasks')`
- 活动: `/activities-feed` 或 `navigateTo('/activities-feed')`
- 我的: `/member/1` 或 `navigateTo('/member/1')`

