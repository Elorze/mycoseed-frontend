# 页面文件夹结构说明

## 当前页面组织（按四个底部导航栏）

### 1️⃣ 社区广场 (Square) 
**底部导航**: 🗺️ 社区广场 → `/`

```
pages/
├── index.vue              # 社区广场主页 (/)
├── community/             # 社区相关
│   ├── [id].vue          # 社区详情 (/community/:id)
│   └── setup.vue         # 社区设置 (/community/setup)
├── dao.vue                # DAO管理 (/dao)
└── market.vue             # 市场 (/market) - 重定向到/tasks
```

### 2️⃣ 任务 (Tasks)
**底部导航**: 📋 任务 → `/tasks`

```
pages/
└── tasks/                 # 任务相关
    ├── index.vue         # 任务列表 (/tasks)
    ├── [id].vue          # 任务详情 (/tasks/:id)
    ├── create.vue        # 创建任务 (/tasks/create)
    └── detail/           # 任务详情子页面
        ├── submit.vue    # 提交任务 (/tasks/detail/submit)
        └── review.vue    # 审核任务 (/tasks/detail/review)
```

### 3️⃣ 活动 (Activities)
**底部导航**: 📅 活动 → `/activities-feed`

```
pages/
├── activities-feed.vue    # 活动流主页 (/activities-feed)
└── activities/            # 活动相关
    ├── index.vue         # 活动列表 (/activities)
    ├── [id].vue          # 活动详情 (/activities/:id)
    └── create.vue        # 创建活动 (/activities/create)
```

### 4️⃣ 我的 (Member)
**底部导航**: 👤 我的 → `/member/1`

```
pages/
├── member/               # 成员相关
│   └── [id].vue         # 个人主页 (/member/:id)
├── profile/              # 个人资料设置
│   └── setup.vue        # 个人资料设置 (/profile/setup)
└── wallet.vue           # 钱包 (/wallet) - 重定向到/member/:id
```

### 🔐 认证相关 (Auth)
**独立页面，不属于底部导航栏**

```
pages/
├── login.vue            # 登录 (/login)
├── verify.vue           # 验证 (/verify)
└── verifyphone.vue      # 手机验证 (/verifyphone)
```

---

## 路由映射表

| 底部导航栏 | 路径 | 对应文件 | 说明 |
|-----------|------|---------|------|
| 🗺️ 社区广场 | `/` | `index.vue` | 社区广场主页 |
| 📋 任务 | `/tasks` | `tasks/index.vue` | 任务列表 |
| 📅 活动 | `/activities-feed` | `activities-feed.vue` | 活动流 |
| 👤 我的 | `/member/1` | `member/[id].vue` | 个人主页 |

---

## 文件组织原则

1. **主要入口页面保持在根目录**: 为了保持路由路径不变
   - `index.vue` → `/` (社区广场)
   - `activities-feed.vue` → `/activities-feed` (活动流)

2. **功能模块组织到文件夹**: 相关功能的子页面组织到对应文件夹
   - `tasks/` - 所有任务相关页面
   - `activities/` - 所有活动相关页面
   - `community/` - 所有社区相关页面
   - `member/` - 成员相关页面
   - `profile/` - 个人资料设置

3. **动态路由使用 `[id].vue` 格式**

4. **嵌套路由使用文件夹结构**

---

## 注意事项

⚠️ **不要移动主要入口文件**: 
- `index.vue` 必须保持在 `pages/` 根目录，否则路由会从 `/` 变成 `/index`
- `activities-feed.vue` 必须保持在根目录，否则路由会改变

✅ **可以组织的文件**:
- 子页面可以移动到对应的功能文件夹中
- 认证相关页面可以组织到 `auth/` 文件夹（但需要更新所有路由引用）

