# Pages 目录结构

## 按四个底部导航栏组织的页面结构

### 🗺️ 社区广场 (Square) - 路径: `/`
**主要入口**: `index.vue` - 社区广场主页

**相关页面**:
- `community/` - 社区相关
  - `[id].vue` - 社区详情页
  - `setup.vue` - 社区设置页
- `dao.vue` - DAO管理页面
- `market.vue` - 市场页面（重定向到/tasks）

---

### 📋 任务 (Tasks) - 路径: `/tasks`
**主要入口**: `tasks/index.vue` - 任务列表页

**相关页面**:
- `tasks/[id].vue` - 任务详情页
- `tasks/create.vue` - 创建任务页
- `tasks/detail/` - 任务详情子页面
  - `submit.vue` - 提交任务页 (`/tasks/detail/submit`)
  - `review.vue` - 审核任务页 (`/tasks/detail/review`)

---

### 📅 活动 (Activities) - 路径: `/activities-feed`
**主要入口**: `activities-feed.vue` - 活动流主页

**相关页面**:
- `activities/` - 活动相关
  - `index.vue` - 活动列表页 (`/activities`)
  - `[id].vue` - 活动详情页 (`/activities/[id]`)
  - `create.vue` - 创建活动页 (`/activities/create`)

---

### 👤 我的 (Member) - 路径: `/member/[id]`
**主要入口**: `member/[id].vue` - 个人主页

**相关页面**:
- `profile/` - 个人资料设置
  - `setup.vue` - 个人资料设置页 (`/profile/setup`)
- `wallet.vue` - 钱包页面（重定向到/member/[id]）

---

### 🔐 认证相关 (Auth)
**独立页面，不属于底部导航栏**:
- `login.vue` - 登录页 (`/login`)
- `verify.vue` - 验证页 (`/verify`)
- `verifyphone.vue` - 手机验证页 (`/verifyphone`)

---

## 路由说明

Nuxt.js 使用基于文件系统的路由，文件路径直接对应URL路径：
- `pages/index.vue` → `/`
- `pages/tasks/index.vue` → `/tasks`
- `pages/member/[id].vue` → `/member/:id`
- `pages/activities-feed.vue` → `/activities-feed`

## 注意事项

1. **保持主要入口在根目录**: 为了保持路由路径不变，主要入口页面（如 `index.vue`, `activities-feed.vue`）保持在 `pages/` 根目录
2. **子页面组织到文件夹**: 相关功能的子页面组织到对应的文件夹中（如 `tasks/`, `activities/`, `member/`）
3. **动态路由**: 使用 `[id].vue` 格式创建动态路由
4. **嵌套路由**: 使用文件夹结构创建嵌套路由（如 `tasks/detail/submit.vue`）


